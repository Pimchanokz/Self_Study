// const multi = `In ES6 
// multi-line is
// okay.`//use `` instead "" can use multi line
// console.log(multi)

const name = "Bob"
const time = "today"
const greet = `Hello ${name + "by"}, how are you ${time}?` //use `` can use ${} to call variables
console.warn(greet)